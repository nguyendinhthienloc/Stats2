# Nhiệm vụ của Trần Lê Anh Tuấn (P2)

Chào Tuấn, đây là hướng dẫn công việc của bạn:

**Nhánh làm việc (Branch):**
Hãy chắc chắn bạn đang ở nhánh của mình trước khi viết code:
`git checkout feature/p2-tuan-ols-ridge`

## Final figure-quality pass (project status: approximately 90%)

Return to the assigned R scripts and fix figure generation at the source. Do
not manually edit the generated PDFs, and do not rename them.

- [ ] Improve the OLS coefficient and diagnostic figures generated by
  `R_models/02_ols.R` (`fig_p2_ols_coef.pdf`, `fig_p2_ols_resid.pdf`, and
  `fig_p2_ols_qq.pdf`).
- [ ] Improve the Ridge CV, coefficient-path, and coefficient figures generated
  by `R_models/02_ridge.R`.
- [ ] Make the selected lambda markers, axes, legends, and coefficient names
  immediately understandable without referring back to the code.
- [ ] Check clipping, overlapping labels, line weights, font sizes, and
  readability at the report's final figure width.
- [ ] Re-run both assigned scripts and visually inspect every output while
  preserving the fitted results and existing filenames.

**Nhiệm vụ chính:** Mô hình OLS & Ridge Regression

**Bạn cần chỉnh sửa các file sau:**
1. Code R: `R_models/02_ols.R` và `R_models/02_ridge.R`
2. Báo cáo LaTeX: `report/sections/02_ols_ridge_lasso.tex` (Phần 2.1 và 2.2)

**Mục tiêu (Phase 2):**
- Đợi Lộc (P1) chạy xong để lấy file `output/shared_data.RData`.
- Code mô hình baseline OLS và tính residual.
- Code mô hình Ridge, dùng Cross Validation (với `foldid` chuẩn từ Lộc) để tìm lambda tốt nhất.
- Vẽ đồ thị CV và đồ thị hệ số shrinkage cho Ridge.
- Viết báo cáo giải thích kết quả cho 2 mô hình này.
