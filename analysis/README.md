# Analysis pipeline

Run `analysis/00_run_all.R` from the repository root. Individual numbered files may be run while developing, but they must remain reproducible in the documented order.

The scripts are scaffolds aligned to the brief. Replace each placeholder with reviewed analysis code and check its corresponding `TODO.md` item only after review.
